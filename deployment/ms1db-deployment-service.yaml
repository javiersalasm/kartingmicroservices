apiVersion: v1
kind: PersistentVolume
metadata:
  name: ms1db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/ms1db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ms1db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms1db-deployment
  labels:
    app: ms1db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: ms1db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ms1db-deployment
        tier: database
    spec:
      containers:
        - name: ms1db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_MS1
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_MS1
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: ms1db-data
      volumes:
        - name: ms1db-data
          persistentVolumeClaim:
            claimName: ms1db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: ms1db-service
  labels:
    app: ms1db-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: ms1db-deployment
    tier: database
  type: LoadBalancer